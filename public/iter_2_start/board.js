define([],function() {
  return {
    create: function(){
      return {
        placePiece: function(col, row, color){
          // do nothing yet
        },
        winner: function(){
          // we haven't implemented the winner yet
          return undefined;
        }
      };
    }
  };
});
